{
  "name": "fs-backend",
  "version": "1.0.0",
  "description": "Fullstack Open part 3 backend",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build:ui": "rm -rf dist/ && cd ../frontend && npm run build && cp -r dist ../backend",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joona Kärkkäinen",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
